import React, { FC } from "react";
import { Box } from "zmp-ui";
import { BoxProps } from "zmp-ui/box";

const DividerSpaceLine: FC = ({

}) => {
  return (
    <div
    className="my-8"     
    />
  );
};

export default DividerSpaceLine;
